# iOS Shell Note

The original project did not include a generated iOS React Native shell.

Use [docs/NATIVE_BOOTSTRAP.md](/Users/re1lk/userstorage/prog/QuestMarks/docs/NATIVE_BOOTSTRAP.md) to generate or reconnect the native shell, then add `GoogleService-Info.plist` only if Firebase mode is required.

