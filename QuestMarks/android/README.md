# Android Shell Note

This folder was present but not scaffolded as a full React Native Android project when work resumed.

Use [docs/NATIVE_BOOTSTRAP.md](/Users/re1lk/userstorage/prog/QuestMarks/docs/NATIVE_BOOTSTRAP.md) to generate or reconnect the native shell, then place Android Firebase config at `android/app/google-services.json` if Firebase mode is required.

